let userName
userName = ['Max', 'Chris']

let userId = (a: string, b: number) => a+ b
userId('2',3)

let target  = document.getElementById('target')
target.onclick = (event: MouseEvent) => event.preventDefault()